$(".comments").html('<%= escape_javascript(render(:partial => "recording", :collection => @recordings) || "No recordings for this site." ) %>');
// var as = audiojs.createAll();
audiojs.events.ready(function() {
    var as = audiojs.createAll();
});
